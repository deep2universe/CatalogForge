# CatalogForge Frontend – Vollständige Spezifikation

## 1. Übersicht

### 1.1 Projektziel

React-basiertes Frontend für die CatalogForge-Plattform – eine KI-gestützte Katalog- und Flyer-Erstellungsanwendung. Das Frontend unterstützt alle Backend-Funktionen und bietet ein professionelles Dashboard mit Pastellfarben-Design.

### 1.2 Zielverzeichnis

```
catForge-frontend/
```

### 1.3 Sprache

- **UI-Texte**: Deutsch
- **Code**: Englisch

---

## 2. Tech-Stack

| Technologie | Version | Zweck |
|-------------|---------|-------|
| React | 18.x | UI-Framework |
| TypeScript | 5.x | Typsicherheit |
| Vite | 5.x | Build-Tool |
| TailwindCSS | 3.x | Styling (Pastellfarben) |
| React Router | 6.x | Navigation |
| TanStack Query | 5.x | Server-State-Management |
| Zustand | 4.x | Client-State-Management |
| Recharts | 2.x | Diagramme |
| React Hook Form | 7.x | Formulare |
| Lucide React | - | Icons |
| React Markdown | - | Markdown-Rendering |


---

## 3. Design-System

### 3.1 Pastellfarben-Palette

```css
/* Primärfarben */
--pastel-blue: #A8D5E5;      /* Primary - Navigation, Buttons */
--pastel-blue-dark: #7BC0D4; /* Primary Hover */
--pastel-green: #B5E5CF;     /* Success, Eco-Style */
--pastel-purple: #D4B5E5;    /* Accent, Premium-Style */
--pastel-yellow: #F5E6A3;    /* Warning, Highlights */
--pastel-red: #E5B5B5;       /* Error, Destructive */
--pastel-orange: #E5CDB5;    /* Dynamic-Style */

/* Neutrals */
--neutral-50: #FAFAFA;
--neutral-100: #F5F5F5;
--neutral-200: #E5E5E5;
--neutral-300: #D4D4D4;
--neutral-600: #525252;
--neutral-800: #262626;
--neutral-900: #171717;

/* Hintergründe */
--bg-primary: #FAFAFA;
--bg-card: #FFFFFF;
--bg-sidebar: #F0F7FA;
```

### 3.2 Typografie

```css
/* Font Family */
font-family: 'Inter', system-ui, sans-serif;

/* Größen */
--text-xs: 0.75rem;    /* 12px */
--text-sm: 0.875rem;   /* 14px */
--text-base: 1rem;     /* 16px */
--text-lg: 1.125rem;   /* 18px */
--text-xl: 1.25rem;    /* 20px */
--text-2xl: 1.5rem;    /* 24px */
--text-3xl: 1.875rem;  /* 30px */
```

### 3.3 Komponenten-Styling

- **Cards**: Weiß mit subtle Shadow, 8px Border-Radius
- **Buttons**: Pastellfarben mit dunklerer Hover-Variante
- **Inputs**: Neutral-200 Border, Focus-Ring in Pastel-Blue
- **Badges**: Pastellfarben-Hintergrund mit dunklerem Text

---

## 4. Projektstruktur

```
catForge-frontend/
├── public/
│   └── favicon.svg
├── src/
│   ├── api/                      # API-Client Layer
│   │   ├── client.ts             # Basis HTTP-Client (fetch)
│   │   ├── products.ts           # Products API Funktionen
│   │   ├── layouts.ts            # Layouts API Funktionen
│   │   ├── skills.ts             # Skills API Funktionen
│   │   ├── pdf.ts                # PDF API Funktionen
│   │   ├── images.ts             # Images API Funktionen
│   │   └── types.ts              # TypeScript Interfaces
│   │
│   ├── components/
│   │   ├── ui/                   # Basis-UI-Komponenten
│   │   │   ├── Button.tsx
│   │   │   ├── Card.tsx
│   │   │   ├── Input.tsx
│   │   │   ├── Select.tsx
│   │   │   ├── Badge.tsx
│   │   │   ├── Modal.tsx
│   │   │   ├── Tabs.tsx
│   │   │   ├── Slider.tsx
│   │   │   ├── Toggle.tsx
│   │   │   ├── Spinner.tsx
│   │   │   └── Toast.tsx
│   │   │
│   │   ├── charts/               # Chart-Komponenten
│   │   │   ├── PieChart.tsx
│   │   │   ├── BarChart.tsx
│   │   │   └── StatCard.tsx
│   │   │
│   │   ├── layout/               # Layout-Komponenten
│   │   │   ├── AppLayout.tsx
│   │   │   ├── Sidebar.tsx
│   │   │   ├── Header.tsx
│   │   │   └── PageContainer.tsx
│   │   │
│   │   └── features/             # Feature-spezifische Komponenten
│   │       ├── products/
│   │       │   ├── ProductCard.tsx
│   │       │   ├── ProductGrid.tsx
│   │       │   ├── ProductSearch.tsx
│   │       │   └── ProductFilter.tsx
│   │       ├── skills/
│   │       │   ├── SkillCard.tsx
│   │       │   ├── SkillDetail.tsx
│   │       │   └── CategoryFilter.tsx
│   │       ├── prompts/
│   │       │   ├── PromptCard.tsx
│   │       │   └── PromptDetail.tsx
│   │       ├── wizard/
│   │       │   ├── StepIndicator.tsx
│   │       │   ├── ProductSelector.tsx
│   │       │   ├── LayoutOptions.tsx
│   │       │   ├── PromptInput.tsx
│   │       │   ├── ImageUpload.tsx
│   │       │   └── ChatInterface.tsx
│   │       └── preview/
│   │           ├── LayoutPreview.tsx
│   │           ├── VariantSelector.tsx
│   │           ├── CodeEditor.tsx
│   │           └── PdfExport.tsx
│   │
│   ├── pages/
│   │   ├── Dashboard/
│   │   │   └── DashboardPage.tsx
│   │   ├── SkillExplorer/
│   │   │   └── SkillExplorerPage.tsx
│   │   ├── PromptExplorer/
│   │   │   └── PromptExplorerPage.tsx
│   │   ├── CatalogWizard/
│   │   │   └── CatalogWizardPage.tsx
│   │   └── CatalogPreview/
│   │       └── CatalogPreviewPage.tsx
│   │
│   ├── hooks/                    # Custom Hooks
│   │   ├── useProducts.ts
│   │   ├── useSkills.ts
│   │   ├── useLayouts.ts
│   │   ├── usePdf.ts
│   │   └── useImageUpload.ts
│   │
│   ├── store/                    # Zustand Stores
│   │   ├── wizardStore.ts
│   │   └── layoutStore.ts
│   │
│   ├── utils/                    # Hilfsfunktionen
│   │   ├── formatters.ts
│   │   └── validators.ts
│   │
│   ├── styles/
│   │   └── globals.css
│   │
│   ├── App.tsx
│   ├── main.tsx
│   └── vite-env.d.ts
│
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
├── tailwind.config.js
├── postcss.config.js
└── .env.example
```


---

## 5. Seiten-Spezifikationen

### 5.1 Dashboard (`/`)

**Zweck**: Übersicht über alle Produkte mit interaktiven Visualisierungen

**Datenquellen**:
- `GET /api/v1/products` → Alle Produkte
- `GET /api/v1/products/categories` → Kategorien
- `GET /api/v1/products/series` → Serien

**Layout**:
```
┌─────────────────────────────────────────────────────────────┐
│  Header: "Dashboard"                                         │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ Produkte │ │Kategorien│ │  Serien  │ │ Elektro  │       │
│  │    12    │ │    4     │ │    5     │ │    2     │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────┐  ┌─────────────────────┐          │
│  │   Kategorien-Chart  │  │    Serien-Chart     │          │
│  │     (Pie Chart)     │  │    (Bar Chart)      │          │
│  └─────────────────────┘  └─────────────────────┘          │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐   │
│  │              Preis-Verteilung (Bar Chart)            │   │
│  └─────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Produkttabelle (sortierbar, filterbar)              │   │
│  │  [Suche] [Kategorie ▼] [Serie ▼]                     │   │
│  │  ─────────────────────────────────────────────────── │   │
│  │  Name          │ Kategorie    │ Serie    │ Preis     │   │
│  │  Actros L 1853 │ Fernverkehr  │ Actros L │ 189.500€  │   │
│  │  ...           │ ...          │ ...      │ ...       │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

**Komponenten**:
- `StatCard`: Zahl + Label + Icon + Trend
- `CategoryPieChart`: Produkte nach Kategorie
- `SeriesBarChart`: Produkte pro Serie
- `PriceDistributionChart`: Preisverteilung
- `ProductTable`: Sortierbare Tabelle mit Filter

**Interaktionen**:
- Klick auf StatCard → Filter aktivieren
- Klick auf Chart-Segment → Tabelle filtern
- Hover auf Charts → Tooltip mit Details
- Klick auf Tabellenzeile → Produkt-Detail-Modal

**Deutsche UI-Texte**:
- "Dashboard"
- "Produkte gesamt"
- "Kategorien"
- "Baureihen"
- "Elektrofahrzeuge"
- "Verteilung nach Kategorie"
- "Produkte pro Baureihe"
- "Preisverteilung"
- "Suchen..."
- "Alle Kategorien"
- "Alle Baureihen"


---

### 5.2 Skill Explorer (`/skills`)

**Zweck**: Skills durchsuchen, verstehen und für Katalog-Erstellung kennenlernen

**Datenquellen**:
- `GET /api/v1/skills` → Alle Skills
- `GET /api/v1/skills/categories` → Kategorien
- `GET /api/v1/skills/{category}` → Skills einer Kategorie

**Layout**:
```
┌─────────────────────────────────────────────────────────────┐
│  Header: "Skill Explorer"                                    │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────┐  ┌─────────────────────────────────────────┐   │
│  │ Filter  │  │  Skill-Grid                              │   │
│  │─────────│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐    │   │
│  │ ○ Alle  │  │  │ LAYOUT  │ │TYPOGRAPHY│ │  COLOR  │    │   │
│  │ ○ Core  │  │  │PRINCIPLES│ │         │ │ THEORY  │    │   │
│  │ ○ Styles│  │  │ core    │ │  core   │ │  core   │    │   │
│  │ ○Formats│  │  │ P: 10   │ │  P: 20  │ │  P: 30  │    │   │
│  │         │  │  └─────────┘ └─────────┘ └─────────┘    │   │
│  │         │  │  ┌─────────┐ ┌─────────┐ ┌─────────┐    │   │
│  │         │  │  │ MODERN  │ │TECHNICAL│ │ PREMIUM │    │   │
│  │         │  │  │ styles  │ │ styles  │ │ styles  │    │   │
│  │         │  │  └─────────┘ └─────────┘ └─────────┘    │   │
│  └─────────┘  └─────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

**Skill-Detail-Modal**:
```
┌─────────────────────────────────────────────────────────────┐
│  LAYOUT_PRINCIPLES                              [X]          │
├─────────────────────────────────────────────────────────────┤
│  Kategorie: core          Priorität: 10                      │
│  ─────────────────────────────────────────────────────────── │
│  Abhängigkeiten: [TYPOGRAPHY] [SPACING]                      │
│  ─────────────────────────────────────────────────────────── │
│  ## Inhalt                                                   │
│                                                              │
│  [Markdown-gerendeter Skill-Content]                         │
│                                                              │
│  ...                                                         │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

**Komponenten**:
- `CategoryFilter`: Radio-Buttons für Kategorien
- `SkillCard`: Name, Kategorie-Badge, Priority-Badge
- `SkillDetailModal`: Vollständiger Content mit Markdown-Rendering
- `DependencyBadge`: Klickbare Abhängigkeits-Tags

**Deutsche UI-Texte**:
- "Skill Explorer"
- "Alle Skills"
- "Kern-Skills"
- "Stil-Skills"
- "Format-Skills"
- "Priorität"
- "Abhängigkeiten"
- "Keine Abhängigkeiten"
- "Skill-Details"


---

### 5.3 Prompt Explorer (`/prompts`)

**Zweck**: Beispiel-Prompts erkunden und als Vorlage für den Wizard nutzen

**Datenquellen**:
- `GET /api/v1/skills/prompts/examples` → Beispiel-Prompts

**Layout**:
```
┌─────────────────────────────────────────────────────────────┐
│  Header: "Prompt Explorer"                                   │
├─────────────────────────────────────────────────────────────┤
│  Filter: [Alle Stile ▼] [Alle Formate ▼]                    │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐│
│  │ Einfache        │ │ Produktvergleich│ │ Umwelt-Flyer    ││
│  │ Produktseite    │ │                 │ │                 ││
│  │─────────────────│ │─────────────────│ │─────────────────││
│  │ Erstellt eine   │ │ Vergleicht      │ │ Kompakter Flyer ││
│  │ übersichtliche  │ │ mehrere         │ │ mit Fokus auf   ││
│  │ Produktseite... │ │ Produkte...     │ │ Nachhaltigkeit  ││
│  │─────────────────│ │─────────────────│ │─────────────────││
│  │ [modern] [A4]   │ │[technical][A4]  │ │ [eco] [DL]      ││
│  │ [Verwenden →]   │ │ [Verwenden →]   │ │ [Verwenden →]   ││
│  └─────────────────┘ └─────────────────┘ └─────────────────┘│
│                                                              │
│  ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐│
│  │ Premium-        │ │ Technisches     │ │ Produktübersicht││
│  │ Broschüre       │ │ Datenblatt      │ │ Grid            ││
│  │ ...             │ │ ...             │ │ ...             ││
│  └─────────────────┘ └─────────────────┘ └─────────────────┘│
└─────────────────────────────────────────────────────────────┘
```

**Prompt-Detail-Modal**:
```
┌─────────────────────────────────────────────────────────────┐
│  Premium-Broschüre                              [X]          │
├─────────────────────────────────────────────────────────────┤
│  Beschreibung:                                               │
│  Hochwertige Broschüre für Premiumfahrzeuge                 │
│  ─────────────────────────────────────────────────────────── │
│  Prompt:                                                     │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ Erstelle eine elegante Broschürenseite für den Actros L ││
│  │ mit ProCabin. Betone den Komfort und die Premium-       ││
│  │ Ausstattung. Verwende großzügige Weißräume und          ││
│  │ hochwertige Typografie.                                  ││
│  └─────────────────────────────────────────────────────────┘│
│  ─────────────────────────────────────────────────────────── │
│  Optionen:                                                   │
│  • Format: A4                                                │
│  • Stil: premium                                             │
│  • Varianten: 2                                              │
│  • Technische Daten: Ja                                      │
│  ─────────────────────────────────────────────────────────── │
│  [        Im Wizard verwenden        ]                       │
└─────────────────────────────────────────────────────────────┘
```

**Komponenten**:
- `StyleFilter`: Dropdown für Stile
- `FormatFilter`: Dropdown für Formate
- `PromptCard`: Titel, Beschreibung, Style/Format-Badges, Verwenden-Button
- `PromptDetailModal`: Vollständiger Prompt mit Optionen

**Interaktionen**:
- "Verwenden" → Navigiert zum Wizard mit vorausgefüllten Daten
- Filter → Echtzeit-Filterung der Karten

**Deutsche UI-Texte**:
- "Prompt Explorer"
- "Beispiel-Prompts"
- "Alle Stile"
- "Alle Formate"
- "Verwenden"
- "Im Wizard verwenden"
- "Optionen"
- "Varianten"
- "Technische Daten"


---

### 5.4 Katalog-Erstellungs-Wizard (`/wizard`)

**Zweck**: Mehrstufiger Wizard zur Katalog-Erstellung mit Chat-Interface und Feedback-Loop

**Datenquellen**:
- `GET /api/v1/products` → Produktauswahl
- `GET /api/v1/products/categories` → Filter
- `GET /api/v1/products/series` → Filter
- `GET /api/v1/products/search?q=` → Suche
- `POST /api/v1/layouts/generate/text` → Text-to-Layout
- `POST /api/v1/layouts/generate/image` → Image-to-Layout
- `POST /api/v1/images/upload` → Bild-Upload

**Wizard-Schritte**:

#### Schritt 1: Produkte auswählen

```
┌─────────────────────────────────────────────────────────────┐
│  Katalog erstellen                                           │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│  [1. Produkte] → [2. Optionen] → [3. Prompt] → [4. Ergebnis]│
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Produkte auswählen                                          │
│  ─────────────────────────────────────────────────────────── │
│  [🔍 Produkt suchen...]  [Kategorie ▼] [Baureihe ▼]         │
│                                                              │
│  Ausgewählt: 2 Produkte                                      │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ ☑ Actros L 1853 ProCabin    │ ☑ eActros 600            ││
│  │   Fernverkehr · 189.500€    │   Elektro · 286.300€     ││
│  │   [Entfernen]               │   [Entfernen]            ││
│  └─────────────────────────────────────────────────────────┘│
│                                                              │
│  Verfügbare Produkte:                                        │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ Actros L │ │ Actros L │ │ Actros F │ │  Arocs   │       │
│  │  1848    │ │  1863    │ │  1845    │ │  3251 K  │       │
│  │ [+ Add]  │ │ [+ Add]  │ │ [+ Add]  │ │ [+ Add]  │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
│                                                              │
│                                        [Weiter →]            │
└─────────────────────────────────────────────────────────────┘
```

#### Schritt 2: Layout-Optionen

```
┌─────────────────────────────────────────────────────────────┐
│  Katalog erstellen                                           │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│  [✓ Produkte] → [2. Optionen] → [3. Prompt] → [4. Ergebnis] │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Layout-Optionen                                             │
│  ─────────────────────────────────────────────────────────── │
│                                                              │
│  Seitenformat:                                               │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐                   │
│  │ A4  │ │ A5  │ │ DL  │ │ A6  │ │  □  │                   │
│  │ ●   │ │     │ │     │ │     │ │     │                   │
│  └─────┘ └─────┘ └─────┘ └─────┘ └─────┘                   │
│  210×297mm                                                   │
│                                                              │
│  Stil:                                                       │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌────────┐│
│  │ Modern  │ │Technical│ │ Premium │ │   Eco   │ │Dynamic ││
│  │   ●     │ │         │ │         │ │         │ │        ││
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └────────┘│
│                                                              │
│  Varianten: [●━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━●] │
│             1                                             5  │
│             Aktuell: 2 Varianten                            │
│                                                              │
│  ☑ Technische Daten einbeziehen                             │
│  ☐ Komplexe Strategie (mehr LLM-Aufrufe, bessere Qualität)  │
│                                                              │
│                              [← Zurück]  [Weiter →]          │
└─────────────────────────────────────────────────────────────┘
```

#### Schritt 3: Prompt-Eingabe (Chat-Interface)

```
┌─────────────────────────────────────────────────────────────┐
│  Katalog erstellen                                           │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│  [✓ Produkte] → [✓ Optionen] → [3. Prompt] → [4. Ergebnis]  │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Beschreiben Sie Ihren Katalog                               │
│  ─────────────────────────────────────────────────────────── │
│                                                              │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ 💬 Chat-Verlauf                                          ││
│  │                                                          ││
│  │  ┌─────────────────────────────────────────────────┐    ││
│  │  │ 🤖 Willkommen! Beschreiben Sie, wie Ihr Katalog │    ││
│  │  │    aussehen soll. Ich generiere dann passende   │    ││
│  │  │    Layout-Varianten für Sie.                    │    ││
│  │  └─────────────────────────────────────────────────┘    ││
│  │                                                          ││
│  └─────────────────────────────────────────────────────────┘│
│                                                              │
│  Optional: Referenzbild hochladen                            │
│  ┌─────────────────────────────────────────────────────────┐│
│  │  📁 Bild hierher ziehen oder klicken zum Auswählen      ││
│  │     (max. 10MB, JPG/PNG)                                ││
│  └─────────────────────────────────────────────────────────┘│
│                                                              │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ Erstelle eine moderne Produktseite für den eActros 600. ││
│  │ Zeige das Hauptbild prominent, liste die wichtigsten    ││
│  │ technischen Daten übersichtlich auf und hebe die        ││
│  │ Umweltvorteile hervor.                                  ││
│  │                                                    📎 🎤││
│  └─────────────────────────────────────────────────────────┘│
│  4.823 / 5.000 Zeichen                                       │
│                                                              │
│  Beispiel-Prompts: [Produktseite] [Vergleich] [Flyer]       │
│                                                              │
│                              [← Zurück]  [Generieren 🚀]     │
└─────────────────────────────────────────────────────────────┘
```


#### Schritt 4: Generierung & Ergebnis (mit Feedback-Loop)

```
┌─────────────────────────────────────────────────────────────┐
│  Katalog erstellen                                           │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│  [✓ Produkte] → [✓ Optionen] → [✓ Prompt] → [4. Ergebnis]   │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Layout-Varianten                                            │
│  ─────────────────────────────────────────────────────────── │
│                                                              │
│  [Variante 1] [Variante 2]                                   │
│                                                              │
│  ┌─────────────────────────────────────────────────────────┐│
│  │                                                          ││
│  │              ┌─────────────────────┐                     ││
│  │              │                     │                     ││
│  │              │   Layout-Preview    │                     ││
│  │              │   (iframe/shadow)   │                     ││
│  │              │                     │                     ││
│  │              │      A4 Format      │                     ││
│  │              │                     │                     ││
│  │              └─────────────────────┘                     ││
│  │                                                          ││
│  │  Zoom: [50%] [75%] [100%] [150%]                        ││
│  │                                                          ││
│  └─────────────────────────────────────────────────────────┘│
│                                                              │
│  ─────────────────────────────────────────────────────────── │
│  Feedback-Loop:                                              │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ 💬 Chat                                                  ││
│  │                                                          ││
│  │  ┌───────────────────────────────────────────────┐      ││
│  │  │ 👤 Erstelle eine moderne Produktseite...      │      ││
│  │  └───────────────────────────────────────────────┘      ││
│  │  ┌───────────────────────────────────────────────┐      ││
│  │  │ 🤖 Layout generiert! 2 Varianten erstellt.    │      ││
│  │  │    Generierungszeit: 4.5s                     │      ││
│  │  └───────────────────────────────────────────────┘      ││
│  │                                                          ││
│  │  ┌─────────────────────────────────────────────────────┐││
│  │  │ Mache das Bild größer und füge mehr Weißraum hinzu  │││
│  │  └─────────────────────────────────────────────────────┘││
│  │                                    [Neu generieren 🔄]   ││
│  └─────────────────────────────────────────────────────────┘│
│                                                              │
│  [← Zurück]  [Diese Variante verwenden →]                    │
└─────────────────────────────────────────────────────────────┘
```

**Wizard-State (Zustand Store)**:
```typescript
interface WizardState {
  currentStep: 1 | 2 | 3 | 4;
  selectedProducts: Product[];
  options: {
    pageFormat: 'A4' | 'A5' | 'DL' | 'A6' | 'SQUARE';
    style: 'modern' | 'technical' | 'premium' | 'eco' | 'dynamic' | null;
    variantCount: number;
    includeSpecs: boolean;
    complexStrategy: boolean;
  };
  prompt: string;
  referenceImage: {
    base64: string;
    mimeType: string;
  } | null;
  chatHistory: ChatMessage[];
  generatedLayout: LayoutResponse | null;
  selectedVariantId: string | null;
  isGenerating: boolean;
  error: string | null;
}
```

**Komponenten**:
- `StepIndicator`: Fortschrittsanzeige
- `ProductSelector`: Multi-Select mit Suche/Filter
- `LayoutOptionsForm`: Format, Style, Varianten
- `ChatInterface`: Nachrichtenverlauf + Input
- `ImageUpload`: Drag&Drop + Preview
- `PromptInput`: Textarea mit Counter
- `GenerationProgress`: Loading-Animation
- `VariantTabs`: Tab-Navigation für Varianten
- `LayoutPreview`: iframe mit HTML/CSS

**Deutsche UI-Texte**:
- "Katalog erstellen"
- "Produkte auswählen"
- "Layout-Optionen"
- "Beschreiben Sie Ihren Katalog"
- "Ergebnis"
- "Weiter"
- "Zurück"
- "Generieren"
- "Neu generieren"
- "Diese Variante verwenden"
- "Ausgewählt: X Produkte"
- "Seitenformat"
- "Stil"
- "Varianten"
- "Technische Daten einbeziehen"
- "Komplexe Strategie"
- "Referenzbild hochladen"
- "Bild hierher ziehen oder klicken"
- "Zeichen"
- "Beispiel-Prompts"
- "Layout-Varianten"
- "Zoom"
- "Generierungszeit"


---

### 5.5 Katalog-Vorschau und Export (`/preview/:layoutId`)

**Zweck**: Generierte Layouts anzeigen, bearbeiten und als PDF exportieren

**Datenquellen**:
- `GET /api/v1/layouts/{id}` → Layout abrufen
- `GET /api/v1/layouts/{id}/variants` → Varianten
- `PUT /api/v1/layouts/{id}` → Layout aktualisieren
- `DELETE /api/v1/layouts/{id}` → Layout löschen
- `POST /api/v1/pdf/generate` → PDF generieren
- `GET /api/v1/pdf/{id}/download` → PDF herunterladen
- `GET /api/v1/pdf/presets` → Print-Presets

**Layout**:
```
┌─────────────────────────────────────────────────────────────┐
│  Katalog-Vorschau                    [Neuer Katalog] [🗑️]   │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌───────────────────────────────┐ ┌───────────────────────┐│
│  │                               │ │ Varianten             ││
│  │                               │ │ ───────────────────── ││
│  │                               │ │ ○ Variante 1          ││
│  │                               │ │ ● Variante 2 ✓        ││
│  │      Layout-Vorschau          │ │                       ││
│  │      (Live-Rendering)         │ │ ───────────────────── ││
│  │                               │ │ Metadaten             ││
│  │         A4 Format             │ │ • Format: A4          ││
│  │                               │ │ • Generiert: 14:30    ││
│  │                               │ │ • Zeit: 4.520ms       ││
│  │                               │ │ • LLM-Calls: 3        ││
│  │                               │ │                       ││
│  │                               │ │ Skills:               ││
│  │                               │ │ [LAYOUT] [TYPOGRAPHY] ││
│  │                               │ │ [MODERN]              ││
│  └───────────────────────────────┘ └───────────────────────┘│
│                                                              │
│  Zoom: [50%] [75%] [●100%] [150%]                           │
│                                                              │
├─────────────────────────────────────────────────────────────┤
│  Code-Editor (optional)                              [▼/▲]  │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ [HTML] [CSS]                                             ││
│  │ ─────────────────────────────────────────────────────── ││
│  │ <div class="catalog-page">                               ││
│  │   <header class="product-header">                        ││
│  │     <h1>eActros 600</h1>                                 ││
│  │     ...                                                  ││
│  │                                          [Speichern 💾]  ││
│  └─────────────────────────────────────────────────────────┘│
│                                                              │
├─────────────────────────────────────────────────────────────┤
│  PDF-Export                                                  │
│  ─────────────────────────────────────────────────────────── │
│  Preset: [Print Professional ▼]                              │
│                                                              │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ Print Professional                                       ││
│  │ • DPI: 300                                               ││
│  │ • Beschnitt: 3mm                                         ││
│  │ • Schnittmarken: Ja                                      ││
│  │ Ideal für professionellen Druck                          ││
│  └─────────────────────────────────────────────────────────┘│
│                                                              │
│  [        PDF generieren und herunterladen 📥        ]       │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

**PDF-Generierung Flow**:
```
[PDF generieren] 
    → POST /pdf/generate {layoutId, variantId, preset}
    → Loading-State mit Spinner
    → Response: {pdfId, downloadUrl}
    → GET /pdf/{id}/download
    → Browser-Download triggern
```

**Komponenten**:
- `LayoutPreview`: iframe/Shadow DOM mit HTML/CSS
- `VariantSelector`: Radio-Liste der Varianten
- `MetadataPanel`: Generierungsdetails
- `SkillBadges`: Verwendete Skills
- `ZoomControls`: Zoom-Buttons
- `CodeEditor`: Tabs für HTML/CSS mit Syntax-Highlighting
- `PresetSelector`: Dropdown mit Preset-Details
- `PdfExportButton`: Generierung + Download

**Deutsche UI-Texte**:
- "Katalog-Vorschau"
- "Neuer Katalog"
- "Varianten"
- "Metadaten"
- "Format"
- "Generiert"
- "Generierungszeit"
- "LLM-Aufrufe"
- "Verwendete Skills"
- "Code-Editor"
- "Speichern"
- "PDF-Export"
- "Preset"
- "Beschnitt"
- "Schnittmarken"
- "PDF generieren und herunterladen"
- "Bildschirm-Ansicht"
- "Standard-Druck"
- "Professioneller Druck"
- "Premium-Druck"


---

## 6. API-Integration

### 6.1 API-Client Konfiguration

```typescript
// src/api/client.ts
const API_BASE_URL = import.meta.env.VITE_API_URL || 'http://localhost:8080/api/v1';

export async function apiClient<T>(
  endpoint: string,
  options?: RequestInit
): Promise<T> {
  const response = await fetch(`${API_BASE_URL}${endpoint}`, {
    headers: {
      'Content-Type': 'application/json',
      ...options?.headers,
    },
    ...options,
  });

  if (!response.ok) {
    const error = await response.json();
    throw new ApiError(error);
  }

  return response.json();
}
```

### 6.2 TypeScript Interfaces

```typescript
// src/api/types.ts

// Products
export interface Product {
  id: number;
  name: string;
  shortDescription: string;
  description: string;
  longDescription: string;
  category: string;
  series: string;
  specs: Record<string, string>;
  highlights: string[];
  imageUrl: string;
  priceEur: number | null;
}

// Layouts
export interface LayoutOptions {
  pageFormat?: 'A4' | 'A5' | 'A6' | 'DL' | 'SQUARE';
  style?: 'modern' | 'technical' | 'premium' | 'eco' | 'dynamic';
  variantCount?: number;
  includeSpecs?: boolean;
  complexStrategy?: boolean;
}

export interface TextToLayoutRequest {
  productIds: number[];
  options?: LayoutOptions;
  prompt: string;
}

export interface ImageToLayoutRequest {
  productIds: number[];
  options?: LayoutOptions;
  prompt?: string;
  imageBase64: string;
  imageMimeType: string;
}

export interface LayoutResponse {
  id: string;
  status: string;
  generatedAt: string;
  pageFormat: string;
  variants: VariantResponse[];
  variantCount: number;
}

export interface VariantResponse {
  id: string;
  html: string;
  css: string;
}

// Skills
export interface Skill {
  name: string;
  category: string;
  content: string;
  dependencies: string[];
  priority: number;
}

export interface ExamplePrompt {
  id: string;
  title: string;
  prompt: string;
  skills: string[];
  style: string;
  format: string;
}

// PDF
export interface PdfGenerateRequest {
  layoutId: string;
  variantId?: string;
  preset?: string;
}

export interface PdfGenerateResponse {
  pdfId: string;
  downloadUrl: string;
}

export interface PrintPreset {
  name: string;
  description: string;
  dpi: number;
  bleedMm: number;
  cropMarks: boolean;
}

// Images
export interface ImageUploadResponse {
  imageId: string;
  url: string;
  mimeType: string;
  expiresAt: string;
}

// Errors
export interface ErrorResponse {
  timestamp: string;
  status: number;
  error: string;
  message: string;
  path: string;
}
```

### 6.3 API-Funktionen

```typescript
// src/api/products.ts
export const productsApi = {
  getAll: (category?: string, series?: string) => 
    apiClient<Product[]>(`/products?${new URLSearchParams({
      ...(category && { category }),
      ...(series && { series }),
    })}`),
  
  getById: (id: number) => 
    apiClient<Product>(`/products/${id}`),
  
  getCategories: () => 
    apiClient<string[]>('/products/categories'),
  
  getSeries: () => 
    apiClient<string[]>('/products/series'),
  
  search: (query: string) => 
    apiClient<Product[]>(`/products/search?q=${encodeURIComponent(query)}`),
};

// src/api/layouts.ts
export const layoutsApi = {
  generateFromText: (request: TextToLayoutRequest) =>
    apiClient<LayoutResponse>('/layouts/generate/text', {
      method: 'POST',
      body: JSON.stringify(request),
    }),

  generateFromImage: (request: ImageToLayoutRequest) =>
    apiClient<LayoutResponse>('/layouts/generate/image', {
      method: 'POST',
      body: JSON.stringify(request),
    }),

  getById: (id: string) =>
    apiClient<LayoutResponse>(`/layouts/${id}`),

  update: (id: string, layout: Partial<LayoutResponse>) =>
    apiClient<LayoutResponse>(`/layouts/${id}`, {
      method: 'PUT',
      body: JSON.stringify(layout),
    }),

  delete: (id: string) =>
    apiClient<void>(`/layouts/${id}`, { method: 'DELETE' }),

  getVariants: (id: string) =>
    apiClient<VariantResponse[]>(`/layouts/${id}/variants`),
};

// src/api/skills.ts
export const skillsApi = {
  getAll: () => 
    apiClient<Skill[]>('/skills'),
  
  getCategories: () => 
    apiClient<string[]>('/skills/categories'),
  
  getByCategory: (category: string) => 
    apiClient<Skill[]>(`/skills/${category}`),
  
  getExamplePrompts: () => 
    apiClient<ExamplePrompt[]>('/skills/prompts/examples'),
  
  reload: () => 
    apiClient<void>('/skills/reload', { method: 'POST' }),
};

// src/api/pdf.ts
export const pdfApi = {
  generate: (request: PdfGenerateRequest) =>
    apiClient<PdfGenerateResponse>('/pdf/generate', {
      method: 'POST',
      body: JSON.stringify(request),
    }),

  download: async (id: string): Promise<Blob> => {
    const response = await fetch(`${API_BASE_URL}/pdf/${id}/download`);
    return response.blob();
  },

  getPresets: () =>
    apiClient<PrintPreset[]>('/pdf/presets'),
};

// src/api/images.ts
export const imagesApi = {
  upload: async (file: File): Promise<ImageUploadResponse> => {
    const formData = new FormData();
    formData.append('file', file);
    
    const response = await fetch(`${API_BASE_URL}/images/upload`, {
      method: 'POST',
      body: formData,
    });
    
    return response.json();
  },

  uploadBase64: (base64Data: string, mimeType: string, filename?: string) =>
    apiClient<ImageUploadResponse>('/images/upload/base64', {
      method: 'POST',
      body: JSON.stringify({ base64Data, mimeType, filename }),
    }),
};
```


---

## 7. React Query Hooks

```typescript
// src/hooks/useProducts.ts
import { useQuery } from '@tanstack/react-query';
import { productsApi } from '../api/products';

export function useProducts(category?: string, series?: string) {
  return useQuery({
    queryKey: ['products', { category, series }],
    queryFn: () => productsApi.getAll(category, series),
  });
}

export function useProduct(id: number) {
  return useQuery({
    queryKey: ['products', id],
    queryFn: () => productsApi.getById(id),
    enabled: !!id,
  });
}

export function useCategories() {
  return useQuery({
    queryKey: ['products', 'categories'],
    queryFn: productsApi.getCategories,
  });
}

export function useSeries() {
  return useQuery({
    queryKey: ['products', 'series'],
    queryFn: productsApi.getSeries,
  });
}

export function useProductSearch(query: string) {
  return useQuery({
    queryKey: ['products', 'search', query],
    queryFn: () => productsApi.search(query),
    enabled: query.length >= 2,
  });
}

// src/hooks/useSkills.ts
import { useQuery } from '@tanstack/react-query';
import { skillsApi } from '../api/skills';

export function useSkills() {
  return useQuery({
    queryKey: ['skills'],
    queryFn: skillsApi.getAll,
  });
}

export function useSkillCategories() {
  return useQuery({
    queryKey: ['skills', 'categories'],
    queryFn: skillsApi.getCategories,
  });
}

export function useSkillsByCategory(category: string) {
  return useQuery({
    queryKey: ['skills', category],
    queryFn: () => skillsApi.getByCategory(category),
    enabled: !!category,
  });
}

export function useExamplePrompts() {
  return useQuery({
    queryKey: ['skills', 'prompts', 'examples'],
    queryFn: skillsApi.getExamplePrompts,
  });
}

// src/hooks/useLayouts.ts
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { layoutsApi } from '../api/layouts';

export function useLayout(id: string) {
  return useQuery({
    queryKey: ['layouts', id],
    queryFn: () => layoutsApi.getById(id),
    enabled: !!id,
  });
}

export function useGenerateFromText() {
  const queryClient = useQueryClient();
  
  return useMutation({
    mutationFn: layoutsApi.generateFromText,
    onSuccess: (data) => {
      queryClient.setQueryData(['layouts', data.id], data);
    },
  });
}

export function useGenerateFromImage() {
  const queryClient = useQueryClient();
  
  return useMutation({
    mutationFn: layoutsApi.generateFromImage,
    onSuccess: (data) => {
      queryClient.setQueryData(['layouts', data.id], data);
    },
  });
}

export function useUpdateLayout() {
  const queryClient = useQueryClient();
  
  return useMutation({
    mutationFn: ({ id, layout }: { id: string; layout: Partial<LayoutResponse> }) =>
      layoutsApi.update(id, layout),
    onSuccess: (data, { id }) => {
      queryClient.setQueryData(['layouts', id], data);
    },
  });
}

export function useDeleteLayout() {
  const queryClient = useQueryClient();
  
  return useMutation({
    mutationFn: layoutsApi.delete,
    onSuccess: (_, id) => {
      queryClient.removeQueries({ queryKey: ['layouts', id] });
    },
  });
}

// src/hooks/usePdf.ts
import { useQuery, useMutation } from '@tanstack/react-query';
import { pdfApi } from '../api/pdf';

export function usePrintPresets() {
  return useQuery({
    queryKey: ['pdf', 'presets'],
    queryFn: pdfApi.getPresets,
  });
}

export function useGeneratePdf() {
  return useMutation({
    mutationFn: pdfApi.generate,
  });
}

export function useDownloadPdf() {
  return useMutation({
    mutationFn: async (id: string) => {
      const blob = await pdfApi.download(id);
      const url = URL.createObjectURL(blob);
      const a = document.createElement('a');
      a.href = url;
      a.download = `layout-${id}.pdf`;
      a.click();
      URL.revokeObjectURL(url);
    },
  });
}
```


---

## 8. State Management (Zustand)

```typescript
// src/store/wizardStore.ts
import { create } from 'zustand';
import { Product, LayoutOptions, LayoutResponse } from '../api/types';

interface ChatMessage {
  id: string;
  role: 'user' | 'assistant';
  content: string;
  timestamp: Date;
}

interface WizardState {
  // Navigation
  currentStep: 1 | 2 | 3 | 4;
  setStep: (step: 1 | 2 | 3 | 4) => void;
  nextStep: () => void;
  prevStep: () => void;

  // Step 1: Products
  selectedProducts: Product[];
  addProduct: (product: Product) => void;
  removeProduct: (productId: number) => void;
  clearProducts: () => void;

  // Step 2: Options
  options: LayoutOptions;
  setOptions: (options: Partial<LayoutOptions>) => void;

  // Step 3: Prompt
  prompt: string;
  setPrompt: (prompt: string) => void;
  referenceImage: { base64: string; mimeType: string } | null;
  setReferenceImage: (image: { base64: string; mimeType: string } | null) => void;

  // Chat
  chatHistory: ChatMessage[];
  addChatMessage: (message: Omit<ChatMessage, 'id' | 'timestamp'>) => void;
  clearChat: () => void;

  // Step 4: Result
  generatedLayout: LayoutResponse | null;
  setGeneratedLayout: (layout: LayoutResponse | null) => void;
  selectedVariantId: string | null;
  setSelectedVariantId: (id: string | null) => void;

  // Status
  isGenerating: boolean;
  setIsGenerating: (value: boolean) => void;
  error: string | null;
  setError: (error: string | null) => void;

  // Reset
  reset: () => void;
}

const initialOptions: LayoutOptions = {
  pageFormat: 'A4',
  style: 'modern',
  variantCount: 2,
  includeSpecs: true,
  complexStrategy: false,
};

export const useWizardStore = create<WizardState>((set, get) => ({
  // Navigation
  currentStep: 1,
  setStep: (step) => set({ currentStep: step }),
  nextStep: () => set((state) => ({ 
    currentStep: Math.min(state.currentStep + 1, 4) as 1 | 2 | 3 | 4 
  })),
  prevStep: () => set((state) => ({ 
    currentStep: Math.max(state.currentStep - 1, 1) as 1 | 2 | 3 | 4 
  })),

  // Products
  selectedProducts: [],
  addProduct: (product) => set((state) => ({
    selectedProducts: state.selectedProducts.some(p => p.id === product.id)
      ? state.selectedProducts
      : [...state.selectedProducts, product],
  })),
  removeProduct: (productId) => set((state) => ({
    selectedProducts: state.selectedProducts.filter(p => p.id !== productId),
  })),
  clearProducts: () => set({ selectedProducts: [] }),

  // Options
  options: initialOptions,
  setOptions: (options) => set((state) => ({
    options: { ...state.options, ...options },
  })),

  // Prompt
  prompt: '',
  setPrompt: (prompt) => set({ prompt }),
  referenceImage: null,
  setReferenceImage: (image) => set({ referenceImage: image }),

  // Chat
  chatHistory: [{
    id: 'welcome',
    role: 'assistant',
    content: 'Willkommen! Beschreiben Sie, wie Ihr Katalog aussehen soll. Ich generiere dann passende Layout-Varianten für Sie.',
    timestamp: new Date(),
  }],
  addChatMessage: (message) => set((state) => ({
    chatHistory: [...state.chatHistory, {
      ...message,
      id: crypto.randomUUID(),
      timestamp: new Date(),
    }],
  })),
  clearChat: () => set({ chatHistory: [] }),

  // Result
  generatedLayout: null,
  setGeneratedLayout: (layout) => set({ generatedLayout: layout }),
  selectedVariantId: null,
  setSelectedVariantId: (id) => set({ selectedVariantId: id }),

  // Status
  isGenerating: false,
  setIsGenerating: (value) => set({ isGenerating: value }),
  error: null,
  setError: (error) => set({ error }),

  // Reset
  reset: () => set({
    currentStep: 1,
    selectedProducts: [],
    options: initialOptions,
    prompt: '',
    referenceImage: null,
    chatHistory: [{
      id: 'welcome',
      role: 'assistant',
      content: 'Willkommen! Beschreiben Sie, wie Ihr Katalog aussehen soll.',
      timestamp: new Date(),
    }],
    generatedLayout: null,
    selectedVariantId: null,
    isGenerating: false,
    error: null,
  }),
}));
```


---

## 9. Routing

```typescript
// src/App.tsx
import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { AppLayout } from './components/layout/AppLayout';
import { DashboardPage } from './pages/Dashboard/DashboardPage';
import { SkillExplorerPage } from './pages/SkillExplorer/SkillExplorerPage';
import { PromptExplorerPage } from './pages/PromptExplorer/PromptExplorerPage';
import { CatalogWizardPage } from './pages/CatalogWizard/CatalogWizardPage';
import { CatalogPreviewPage } from './pages/CatalogPreview/CatalogPreviewPage';

const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 5 * 60 * 1000, // 5 Minuten
      retry: 1,
    },
  },
});

export function App() {
  return (
    <QueryClientProvider client={queryClient}>
      <BrowserRouter>
        <Routes>
          <Route element={<AppLayout />}>
            <Route path="/" element={<DashboardPage />} />
            <Route path="/skills" element={<SkillExplorerPage />} />
            <Route path="/prompts" element={<PromptExplorerPage />} />
            <Route path="/wizard" element={<CatalogWizardPage />} />
            <Route path="/preview/:layoutId" element={<CatalogPreviewPage />} />
            <Route path="*" element={<Navigate to="/" replace />} />
          </Route>
        </Routes>
      </BrowserRouter>
    </QueryClientProvider>
  );
}
```

---

## 10. Navigation (Sidebar)

```typescript
// src/components/layout/Sidebar.tsx
const navigationItems = [
  {
    path: '/',
    label: 'Dashboard',
    icon: LayoutDashboard,
  },
  {
    path: '/skills',
    label: 'Skill Explorer',
    icon: Sparkles,
  },
  {
    path: '/prompts',
    label: 'Prompt Explorer',
    icon: MessageSquare,
  },
  {
    path: '/wizard',
    label: 'Katalog erstellen',
    icon: Wand2,
    highlight: true, // Primary Action
  },
];
```

**Sidebar-Layout**:
```
┌─────────────────────┐
│  🏭 CatalogForge    │
│                     │
├─────────────────────┤
│  📊 Dashboard       │
│  ✨ Skill Explorer  │
│  💬 Prompt Explorer │
│                     │
│  ━━━━━━━━━━━━━━━━━  │
│                     │
│  🪄 Katalog         │
│     erstellen       │
│     (Primary CTA)   │
│                     │
├─────────────────────┤
│                     │
│  Backend: ● Online  │
│  v1.0.0             │
└─────────────────────┘
```


---

## 11. Konfigurationsdateien

### 11.1 package.json

```json
{
  "name": "catforge-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.22.0",
    "@tanstack/react-query": "^5.24.0",
    "zustand": "^4.5.0",
    "recharts": "^2.12.0",
    "react-hook-form": "^7.50.0",
    "react-markdown": "^9.0.1",
    "lucide-react": "^0.330.0",
    "clsx": "^2.1.0",
    "tailwind-merge": "^2.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.55",
    "@types/react-dom": "^18.2.19",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "@vitejs/plugin-react": "^4.2.1",
    "autoprefixer": "^10.4.17",
    "eslint": "^8.56.0",
    "eslint-plugin-react-hooks": "^4.6.0",
    "eslint-plugin-react-refresh": "^0.4.5",
    "postcss": "^8.4.35",
    "tailwindcss": "^3.4.1",
    "typescript": "^5.3.3",
    "vite": "^5.1.0"
  }
}
```

### 11.2 vite.config.ts

```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  server: {
    port: 3000,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true,
      },
    },
  },
});
```

### 11.3 tailwind.config.js

```javascript
/** @type {import('tailwindcss').Config} */
export default {
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
  theme: {
    extend: {
      colors: {
        pastel: {
          blue: {
            DEFAULT: '#A8D5E5',
            dark: '#7BC0D4',
            light: '#D4EAF2',
          },
          green: {
            DEFAULT: '#B5E5CF',
            dark: '#8AD4B5',
            light: '#DAF2E7',
          },
          purple: {
            DEFAULT: '#D4B5E5',
            dark: '#B98AD4',
            light: '#E9DAF2',
          },
          yellow: {
            DEFAULT: '#F5E6A3',
            dark: '#E8D47A',
            light: '#FAF3D1',
          },
          red: {
            DEFAULT: '#E5B5B5',
            dark: '#D48A8A',
            light: '#F2DADA',
          },
          orange: {
            DEFAULT: '#E5CDB5',
            dark: '#D4B08A',
            light: '#F2E6DA',
          },
        },
        sidebar: '#F0F7FA',
      },
      fontFamily: {
        sans: ['Inter', 'system-ui', 'sans-serif'],
      },
      boxShadow: {
        card: '0 1px 3px 0 rgb(0 0 0 / 0.05), 0 1px 2px -1px rgb(0 0 0 / 0.05)',
        'card-hover': '0 4px 6px -1px rgb(0 0 0 / 0.07), 0 2px 4px -2px rgb(0 0 0 / 0.07)',
      },
    },
  },
  plugins: [],
};
```

### 11.4 .env.example

```bash
# API Configuration
VITE_API_URL=http://localhost:8080/api/v1
```


---

## 12. Error Handling

### 12.1 API Error Handler

```typescript
// src/api/errors.ts
export class ApiError extends Error {
  constructor(
    public response: ErrorResponse,
  ) {
    super(response.message);
    this.name = 'ApiError';
  }

  get status() {
    return this.response.status;
  }

  get isNotFound() {
    return this.status === 404;
  }

  get isValidationError() {
    return this.status === 400;
  }

  get isServerError() {
    return this.status >= 500;
  }
}
```

### 12.2 Error-Szenarien und UI-Feedback

| HTTP Status | Szenario | UI-Feedback |
|-------------|----------|-------------|
| 400 | Validierungsfehler | Inline-Fehlermeldung am Feld |
| 404 | Ressource nicht gefunden | "Nicht gefunden"-Seite oder Toast |
| 413 | Datei zu groß | "Datei überschreitet 10MB" Toast |
| 502 | Gemini API Fehler | "KI-Service nicht erreichbar" Modal |
| 500 | Server-Fehler | "Ein Fehler ist aufgetreten" Toast |

### 12.3 Toast-Komponente

```typescript
// Deutsche Fehlermeldungen
const errorMessages: Record<number, string> = {
  400: 'Ungültige Eingabe. Bitte überprüfen Sie Ihre Daten.',
  404: 'Die angeforderte Ressource wurde nicht gefunden.',
  413: 'Die Datei ist zu groß. Maximale Größe: 10MB.',
  502: 'Der KI-Service ist momentan nicht erreichbar. Bitte versuchen Sie es später erneut.',
  500: 'Ein unerwarteter Fehler ist aufgetreten.',
};
```

---

## 13. Responsive Design

### Breakpoints

```css
/* Tailwind Standard-Breakpoints */
sm: 640px   /* Tablet Portrait */
md: 768px   /* Tablet Landscape */
lg: 1024px  /* Desktop */
xl: 1280px  /* Large Desktop */
2xl: 1536px /* Extra Large */
```

### Layout-Anpassungen

| Viewport | Sidebar | Content |
|----------|---------|---------|
| < 768px | Collapsed (Hamburger) | Full Width |
| ≥ 768px | Fixed 240px | Flex |
| ≥ 1280px | Fixed 280px | Max-Width Container |

---

## 14. Accessibility (A11y)

- **Keyboard Navigation**: Alle interaktiven Elemente per Tab erreichbar
- **Focus States**: Sichtbare Focus-Ringe (Pastel-Blue)
- **ARIA Labels**: Für Icons und interaktive Elemente
- **Color Contrast**: Mindestens 4.5:1 für Text
- **Screen Reader**: Semantisches HTML, aria-live für Toasts
- **Reduced Motion**: `prefers-reduced-motion` respektieren

---

## 15. Performance-Optimierungen

- **Code Splitting**: Lazy Loading für Seiten
- **React Query Caching**: 5 Minuten staleTime
- **Image Optimization**: Lazy Loading für Produktbilder
- **Bundle Size**: Tree-Shaking, nur benötigte Lucide Icons
- **Debouncing**: Suche mit 300ms Debounce

```typescript
// Lazy Loading Beispiel
const DashboardPage = lazy(() => import('./pages/Dashboard/DashboardPage'));
const CatalogWizardPage = lazy(() => import('./pages/CatalogWizard/CatalogWizardPage'));
```

---

## 16. Entwicklungs-Workflow

### Befehle

```bash
# Installation
npm install

# Entwicklungsserver starten (Port 3000)
npm run dev

# Production Build
npm run build

# Build-Preview
npm run preview

# Linting
npm run lint
```

### Voraussetzungen

- Node.js 18+
- Backend läuft auf `http://localhost:8080`

### Proxy-Konfiguration

Vite proxied `/api/*` Requests automatisch zum Backend, sodass CORS im Development kein Problem ist.


---

## 17. Komponenten-Bibliothek (UI)

### 17.1 Button

```typescript
// Varianten
type ButtonVariant = 'primary' | 'secondary' | 'ghost' | 'danger';
type ButtonSize = 'sm' | 'md' | 'lg';

// Farben
primary: bg-pastel-blue hover:bg-pastel-blue-dark
secondary: bg-white border-neutral-200 hover:bg-neutral-50
ghost: bg-transparent hover:bg-neutral-100
danger: bg-pastel-red hover:bg-pastel-red-dark
```

### 17.2 Card

```typescript
// Basis-Card mit Hover-Effekt
className="bg-white rounded-lg shadow-card hover:shadow-card-hover transition-shadow"
```

### 17.3 Badge

```typescript
// Varianten für Kategorien/Stile
type BadgeVariant = 'default' | 'blue' | 'green' | 'purple' | 'yellow' | 'orange';

// Mapping für Stile
const styleBadgeColors = {
  modern: 'blue',
  technical: 'default',
  premium: 'purple',
  eco: 'green',
  dynamic: 'orange',
};
```

### 17.4 Input

```typescript
// Basis-Styling
className="w-full px-3 py-2 border border-neutral-200 rounded-lg 
           focus:outline-none focus:ring-2 focus:ring-pastel-blue focus:border-transparent"
```

### 17.5 Modal

```typescript
// Overlay + zentrierter Content
// Schließen per Escape oder Klick außerhalb
// Focus-Trap für Accessibility
```

---

## 18. Vollständige Seitenübersicht

| Route | Seite | Hauptfunktion |
|-------|-------|---------------|
| `/` | Dashboard | Produkt-Statistiken und Visualisierungen |
| `/skills` | Skill Explorer | Skills durchsuchen und verstehen |
| `/prompts` | Prompt Explorer | Beispiel-Prompts erkunden |
| `/wizard` | Katalog-Wizard | Mehrstufige Katalog-Erstellung |
| `/preview/:id` | Katalog-Vorschau | Layout-Preview und PDF-Export |

---

## 19. Backend-Abhängigkeiten

Das Frontend setzt voraus, dass das Backend unter `http://localhost:8080` läuft und folgende Endpoints bereitstellt:

### Products API
- ✅ `GET /api/v1/products`
- ✅ `GET /api/v1/products/{id}`
- ✅ `GET /api/v1/products/categories`
- ✅ `GET /api/v1/products/series`
- ✅ `GET /api/v1/products/search`

### Skills API
- ✅ `GET /api/v1/skills`
- ✅ `GET /api/v1/skills/categories`
- ✅ `GET /api/v1/skills/{category}`
- ✅ `GET /api/v1/skills/prompts/examples`
- ✅ `POST /api/v1/skills/reload`

### Layouts API
- ✅ `POST /api/v1/layouts/generate/text`
- ✅ `POST /api/v1/layouts/generate/image`
- ✅ `GET /api/v1/layouts/{id}`
- ✅ `PUT /api/v1/layouts/{id}`
- ✅ `DELETE /api/v1/layouts/{id}`
- ✅ `GET /api/v1/layouts/{id}/variants`

### PDF API
- ✅ `POST /api/v1/pdf/generate`
- ✅ `GET /api/v1/pdf/{id}/download`
- ✅ `GET /api/v1/pdf/presets`

### Images API
- ✅ `POST /api/v1/images/upload`
- ✅ `POST /api/v1/images/upload/base64`
- ✅ `GET /api/v1/images/{imageId}`

---

## 20. Zusammenfassung

Diese Spezifikation definiert ein vollständiges React-Frontend für CatalogForge mit:

- **5 Hauptseiten**: Dashboard, Skill Explorer, Prompt Explorer, Katalog-Wizard, Katalog-Vorschau
- **Pastellfarben-Design**: Professionelles, freundliches UI
- **Vollständige API-Integration**: Alle Backend-Endpoints werden genutzt
- **Chat-Interface mit Feedback-Loop**: Interaktive Katalog-Erstellung
- **Deutsche UI**: Alle Texte in Deutsch
- **Moderne Tech-Stack**: React 18, TypeScript, Vite, TailwindCSS, React Query, Zustand

Das Frontend ist speziell auf das bestehende CatalogForge-Backend ausgelegt und implementiert keine neuen Funktionen, sondern nutzt ausschließlich die vorhandenen REST-Endpoints.
